# Core scientific stack (versions used in the initial research environment are
# recorded in reports/00_repository_audit.md; pins are floors, not exact, so the
# framework also runs inside QuantConnect Research containers).
numpy>=1.26
pandas>=2.1
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.8
PyYAML>=6.0

# Research / dev tooling
pytest>=7.4
nbformat>=5.9
jupyter>=1.0
